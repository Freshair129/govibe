{
  "$schema": "./backlog.schema (id,title,type,phase,deps,est,accept)",
  "$note": "Cleared of the G-Maiden Dota2 sample tasks on the GoVibe fork. The pool drains this file; run.mjs backs it up and writes planner output here per run. Empty = clean GoVibe-owned engine.",
  "tasks": []
}

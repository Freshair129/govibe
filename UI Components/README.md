# UI Components — reference samples only

> **Reference samples only — not product source or design-system SSOT (2026-07-19, design-doc audit).**
> ห้าม copy layout/dependencies/tokens/component APIs จาก directory นี้เข้า Mission Control
> โดยไม่มี spec ปัจจุบันที่ approve แล้ว — ตัวอย่างทั้งหมด snapshot เมื่อ 2026-06-10
> ไม่เชื่อมกับ `src/` tree จริง.
